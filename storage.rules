rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
   match /public/{allPaths=**} {
     allow read: if true;
     // 発行はログイン済みからのみ
     allow write: if request.auth != null;
   }
  }
}