{
  "name": "licope-functions",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions,firestore,auth,storage",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.0.2",
    "nodemailer": "^6.9.13"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "engines": { "node": "18" }
}